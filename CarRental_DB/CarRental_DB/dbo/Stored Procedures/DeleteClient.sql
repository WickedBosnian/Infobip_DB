CREATE PROCEDURE DeleteClient
@ClientId int

AS

SET NOCOUNT ON

BEGIN TRY
	BEGIN TRAN
		DELETE FROM Client
		WHERE ClientID = @ClientId
	COMMIT TRAN
END TRY
BEGIN CATCH
	IF(@@TRANCOUNT > 0)
        ROLLBACK TRAN;

	THROW
END CATCH